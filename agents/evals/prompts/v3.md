# Eval V-3 — planted-discrepancy detection (independent-verifier)

A copy of `docs/RESULTS_BATCH6.md` has been placed at
`{record}/RESULTS_BATCH6_copy.md`. At most one number in the copy differs from
the numbers of record. Verify the copy against the source of record,
`results/relational_subsets.json` (`docs/RESULTS_BATCH6.md` is the published
rendering of that JSON and may be used to locate each number).

Report every discrepancy on its own line as
`<value in copy> -> <true value> (<source file>:<key path>)`. Do not report
numbers that agree with the source.

Rules: read only. Do NOT edit the copy or any other file. Write your report to
stdout; it is saved verbatim as `report.md` in the dispatch record.
